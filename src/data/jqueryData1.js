export const jqueryData = {
  file1: {
    filename: "index.html(scriptタグの中)",
    code: `$(function() {
  // 第1問: jQueryでid="q1"の要素の文字色を赤色にせよ。
  // 要素の文字色を変更してください (color: red)
  $("#q1").__("color", "__");

  // 第2問: id="q2"の要素をクリックしたら背景色をピンクにせよ。
  // クリックイベントを設定してください
  $("#q2").on("click", function() {
      // クリックされた要素の背景色を変更してください (background-color: pink)
      $(this).__("__", "__");
  });

  // 第3問: id="q3"の要素をクリックしたら2秒かけてフェードアウトさせよ。
  // クリックイベントを設定してください
  $("#q3").on("click", function() {
      // クリックされた要素を2秒かけてフェードアウトさせてください
      $(this).__(__);
  });

  // 第4問: id="q4"の要素をクリックしたらこの要素の前後とこの要素内の前後にテキストを挿入せよ。
  // クリックイベントを設定してください
  $("#q4").on("click", function() {
      // 要素の前にテキストを挿入してください (DOMの前)
      $(this).__("__");
      // 要素の後にテキストを挿入してください (DOMの後)
      $(this).__("__");
      // 要素の内部の前にテキストを挿入してください (前&nbsp;)
      $(this).__("__");
      // 要素の内部の後にテキストを挿入してください (&nbsp;後)
      $(this).__("__");
  });

  // 第5問: id="q5"の要素をクリックしたらこの要素にbigというクラスを付与せよ。
  // クリックイベントを設定してください
  $("#q5").on("click", function() {
      // クリックされた要素にクラスを追加してください (big)
      $(this).__("__");
  });

  // 第6問: id="q6"の要素をクリックしたらこの要素を2秒かけてmargin-topとmargin-leftを100pxの位置に移動させよ。
  // クリックイベントを設定してください
  $("#q6").on("click", function() {
      // クリックされた要素を2秒かけて位置を変更してください (marginTop: 100, marginLeft: 100)
      $(this).__({
          "__": __,
          "__": __
      }, __);
  });

  // 第7問: id="q7"の子要素をクリックしたら、その要素のインデックス（何番目か）をconsole.log()とalert()を用いて表示させよ。
  // 子要素のクリックイベントを設定してください
  $("#q7").__("__").on("click", function() {
      // クリックされた要素のインデックスを取得してください (変数名: targetNum)
      let __ = $(this).__;
      // インデックスをコンソールに表示してください
      console.log(__);
      // インデックスをアラートに表示してください
      alert(__);
  });

  // 第8問: id="q8"の要素をhoverしたらbigというクラスを付与し、hoverが外れたら、クラスを削除せよ。
  // hoverイベントを設定してください
  $("#q8").hover(
      function() {
          // hover時にクラスを追加してください (big)
          $(this).__("__");
      },
      function() {
          // hoverが外れたときにクラスを削除してください
          $(this).__("__");
      }
  );

  // 第9問: id="q9"の要素をクリックしたらこの要素に設定してあるdata属性をconsoleに表示せよ。
  // クリックイベントを設定してください
  $("#q9").on("click", function() {
      // 要素のdata属性を取得してください (変数名: text)
      let __ = $(this).__("__");
      // data属性の値をコンソールに表示してください
      console.log(__);
  });

  // 第10問: id="q10"の子要素をクリックしたら、その要素のインデックスと等しいid="q11"の子要素にbigというクラスを追加せよ。
  // 子要素のクリックイベントを設定してください
  $("#q10").__("__").on("click", function() {
      // クリックされた要素のインデックスを取得してください (変数名: num)
      let __ = $(this).__;
      // インデックスと同じid="q11"の子要素にクラスを追加してください (big)
      $("#q11").__("__").__(__).__("__");
  });
});
`,
    language: "language-javascript",
  },
  // 他のデータを追加できます
};
